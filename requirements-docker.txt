# Adapter-specific dependencies for wesense-ingester-govaq
# Core dependencies (clickhouse-connect, paho-mqtt, reverse_geocoder) are
# provided by wesense-ingester-core installed separately.

requests>=2.31.0

# Ed25519 signing — needed by wesense-ingester-core's signing module.
# Normally comes via the [p2p] extra but we don't install that since
# this ingester doesn't need eclipse-zenoh.
cryptography>=42.0.0
